/** Mock current user — replace with real auth session when ready. */
export const currentUser = {
  name: "syntax",
  email: "syntax@dev.io",
  avatarUrl: "https://github.com/shadcn.png",
  avatarFallback: "SX",
} as const;
